@main

# Serial Print Example
# Demonstrates Serial communication

const int SENSOR_PIN = 0

on start {
    print("Starting sensor monitoring...")
}

on loop {
    mut int value = analogRead(SENSOR_PIN)
    print(value)
    delay(500)
}

